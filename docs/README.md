Images used by the top-level README.

The glasses shots are real captures composited over a photo, since a raw
capture is a transparent panel on black and does not show what wearing it is
like. The photo is CC0; `mockups/mockup.py` regenerates them.
